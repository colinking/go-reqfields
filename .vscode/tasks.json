{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "go: refresh lint",
      "type": "shell",
      "command": "pwd && echo '...' && go generate ./... golangci-lint cache clean && echo 'done!'",
      "problemMatcher": ["$go"]
    }
  ]
}
